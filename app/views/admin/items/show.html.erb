<main>
  <div class = "container px-5">
    <div class = "row">
      <h5>商品詳細</h5>
    </div>

    <div class = "row">
      <div class = "col-auto">
        <%= image_tag @item.get_item_image(200,200) %>
      </div>
      <div class = "col-auto">
        <p>
          商品名　　
          <span><%= @item.name %></span>
        </p>
        <p>
          商品説明　
          <span><%= @item.introduction %></span>
        </p>
        <p>
          ジャンル　
          <span><%= @item.genre.name %></span>
        </p>
        <p>
          税込価格　
          <span><%= (@item.price+@item.price*0.1).floor.to_s(:delimited) %>
          (<%= @item.price.floor.to_s(:delimited) %>)</snap>
          円 <br>
          (税抜価格)
        </p>
        <p> 販売ステータス
          <% if @item.is_active? %>
        　   <span>販売中</span>
          <% else %>
            <span>販売停止中</span>
          <% end %>
        </p>
      </div>
　　</div>

　　<div class ="row justify-content-center">
　　  <%= link_to "編集する", edit_admin_item_path(@item.id), class: "btn btn-sm btn-success" %>
　　</div>
  </div>
</main>